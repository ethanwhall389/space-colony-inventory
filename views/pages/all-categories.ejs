<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title><%= title %></title>
  </head>
  <body>
    <h1><%= title %></h1>
    <% if (locals.categories) { %>
    <ul>
      <% categories.forEach((category) => { %>
      <li><%= category %></li>
      <% }) %>
    </ul>
    <% } else { %>
    <h2>No categories in your inventory</h2>
    <% } %>
  </body>
</html>
